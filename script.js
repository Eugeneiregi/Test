// function take(number, length, retake) {
//     number = number + "";
//     if (number.length < length) {
//         for (var i = number.length; i < length; i += 1) {
//             number = retake + number;
//         }
//     }

//     return number;
// }


function myFunction(){
    var take = prompt("Put a number");
    var takeIncreamented = new String();
    if(isNaN("take")||take<=0){
    return(take+1);}
  }
  myFunction();